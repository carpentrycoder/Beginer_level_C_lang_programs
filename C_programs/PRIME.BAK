#include<stdio.h>
#include<conio.h>
void main()
{
int num,deno;
clrscr();
printf("enter the no.\n");
scanf("%d",&num);
deno=2;
while(num%deno!=0)
{
 deno++;
}
if(deno==num)
{
printf("it's prime number");
}
else
{
 printf("it's not prime no.");
}
getch();

}