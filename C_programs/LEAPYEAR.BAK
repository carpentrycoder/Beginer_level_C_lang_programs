#include<stdio.h>
#include<conio.h>
void main()
{
int y;
clrscr();
printf("enter the year\n");
scanf("%d",&y);
if(y%100==0)
{
 if(y%400==0)
 {printf("%d is leap year",y);
 }
 else
 {printf("%d is not leap year",y);
 }
}
else
{
 if (y%4==0)
 {printf("%d is leap year",y);
 }
 else
 {printf("%d is not leap year",y);
 }
}
getch();
}